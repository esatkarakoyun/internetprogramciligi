<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo App</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <div class="card mt-3">
            <div class="card-header">Todo App</div>
            <div class="card-body">
                <form id="taskForm">
                    <div class="input-group">
                        <input type="text" id="txtTaskName" class="form-control" placeholder="Add a task">
                        <button type="submit" class="btn btn-primary">Ekle</button>
                    </div>
                </form>
            </div>
            <div class="card-footer">
                <button id="btnClear" class="btn btn-danger btn-sm float-right mb-3">Temizle</button>
                <ul id="task-list" class="list-group"></ul>
            </div>
        </div>
    </div>

    <script>
        let gorevListesi = [
            {"id": 1, "gorevAdi": "Görev 1"},
            {"id": 2, "gorevAdi": "Görev 2"}
        ];

        function displayTasks() {
            const ul = document.getElementById("task-list");
            ul.innerHTML = gorevListesi.map(gorev => 
                `<li class="list-group-item">
                    <input type="checkbox" id="${gorev.id}"> <label for="${gorev.id}">${gorev.gorevAdi}</label>
                </li>`
            ).join('');
        }

        document.getElementById("taskForm").addEventListener("submit", function(event) {
            event.preventDefault();
            const taskName = document.getElementById("txtTaskName").value.trim();
            if (taskName) {
                gorevListesi.push({id: gorevListesi.length + 1, gorevAdi: taskName});
                document.getElementById("txtTaskName").value = '';
                displayTasks();
            }
        });

        document.getElementById("btnClear").addEventListener("click", function() {
            gorevListesi = [];
            displayTasks();
        });

        displayTasks();
    </script>
</body>
</html>